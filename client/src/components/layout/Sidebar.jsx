{/* <div className="sidebar-section">
  <h3 className="sidebar-heading">Communication</h3>
  <ul>
    <li>
      <NavLink to="/chats" className="sidebar-link" activeClassName="active">
        <ChatBubbleLeftRightIcon className="sidebar-icon" />
        <span>Messages</span>
        {unreadMessages > 0 && (
          <span className="sidebar-badge">{unreadMessages}</span>
        )}
      </NavLink>
    </li>
    <li>
      <NavLink to="/video-sessions" className="sidebar-link" activeClassName="active">
        <VideoCameraIcon className="sidebar-icon" />
        <span>Video Sessions</span>
        {upcomingVideoSessions > 0 && (
          <span className="sidebar-badge">{upcomingVideoSessions}</span>
        )}
      </NavLink>
    </li>
  </ul>
</div> */}